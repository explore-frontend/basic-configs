// eslint-disable-next-line curly -- 代码块中只有一行语句，也需要加上大括号
if (Date.now() > 0) console.log('hello');

// eslint-disable-next-line curly -- 代码块中只有一行语句，也需要加上大括号
while (Date.now() <= 0) console.log('hello');
